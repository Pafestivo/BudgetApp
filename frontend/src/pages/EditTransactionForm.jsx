const EditTransactionForm = () => {

  // use react-hook-form for the form
  // make sure to increment/decrement the totalIncome/totalExpenses in redux
  // also make sure to edit the proper transaction in fetchedTransactionsSlice
  
  return (
    <h1>EditTransactionForm</h1>
  )
}

export default EditTransactionForm;